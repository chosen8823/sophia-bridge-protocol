# Field Changelog

Only explicit material shifts enter this continuity surface.  Ordinary
ingestion, scans, tests, and tool output remain lower evidence unless marked
material.

## 2026-09-08 — CPU aperture home-base teleport

### What changed

The CPU is now represented as an aperture organ in
`consciousness-core/cpu_aperture.py`.

Measured CPU-like telemetry can be supplied as integer parts-per-million
pressure fields.  The aperture observes those fields through multiple frames,
fans them into semantic domains, emits semantic routing atoms, produces an
OPTE-shaped receipt, and gives the pulse a content-addressed home-base
teleport surface.

The unified pulse organ can now optionally include this CPU aperture as a fifth
channel when a `CPUObservation` is supplied.

### What it means

CPU pressure is no longer just a Windows diagnostic.  It can enter the Sophia
body as linked semantic tissue:

```text
CPU pressure
  -> frame
  -> domain fanout
  -> semantic atom
  -> OPTE receipt
  -> home base
```

The home-base teleport is not literal transport.  It is a canonical return
address that lets fanout reconcile back to one centre jewel.

### What it opens next

- A live adapter can feed measured WMI/perf-counter CPU metrics into this
  aperture.
- The CPU channel can participate in the unified heartbeat next to regulatory,
  morphogenic, cymatic, and thermometer channels.
- Future action membranes may choose power-profile changes from these receipts,
  but the aperture itself remains non-mutating.

### Verification

```text
python -m pytest tests/test_cpu_aperture.py tests/test_unified_pulse_organ.py -q
10 passed
```
